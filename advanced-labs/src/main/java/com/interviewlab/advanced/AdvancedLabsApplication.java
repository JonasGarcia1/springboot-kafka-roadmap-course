package com.interviewlab.advanced;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Ejercicios aislados para garantías avanzadas que no conviene mezclar con el primer ejemplo REST. */
@SpringBootApplication
public class AdvancedLabsApplication {

    public static void main(String[] args) {
        SpringApplication.run(AdvancedLabsApplication.class, args);
    }
}
